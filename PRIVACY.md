# Surf Wax 隐私政策

生效日期：2026 年 9 月 24 日

Surf Wax 是在用户浏览器内运行的 Side Panel 智能体。用户自行选择 AI 模型服务并提供凭据。扩展的用途是按用户指令读取和操作浏览器；开发者不运营接收对话或网页内容的中转服务器。

## 处理的数据

- 用户输入的消息、模型回复、自定义提示词、模型配置及 API 凭据。
- 智能体执行任务时取得的标签页、网页、可访问性树、截图、浏览器存储、Cookie、网络请求及其他用户指令涉及的数据。可取得的范围取决于任务、Chrome 权限和浏览器实际提供的 API。
- 完整的工具输入、输出、错误、模型用量和请求状态日志。网页内容和工具结果**不会被扩展脱敏、过滤或改写**。

## 存储和传输

模型配置和凭据保存在本机的 `chrome.storage.local`；对话及事件日志保存在本机 IndexedDB。扩展不提供开发者运营的同步服务，也不主动把这些本地记录上传给开发者。

发送模型请求时，扩展把任务所需的消息、历史上下文和工具结果直接发送到用户选择的 AI 服务或自定义兼容端点。网页内容、截图、Cookie 或请求内容可能出现在这些消息或工具结果中。启用 Jev 消息选择后，扩展还会把相应对话上下文发送到用户配置的 Jev 服务。各服务对收到的数据如何保存和使用，由其自身政策决定；用户应在配置前查看所选服务的条款。扩展不会自行隐去敏感内容。

设置页会从 `models.dev` 获取模型目录。该请求不附带本地对话日志或 API 凭据。智能体按用户任务访问网页、下载文件或调用浏览器 API 时，相关网站或服务也可能接收正常浏览器请求。用户自定义端点可使用本地 HTTP 地址；向非本地 HTTP 端点传输内容时没有 HTTPS 保护，用户应优先使用可信的 HTTPS 服务。

## 保留和删除

对话和日志留在当前 Chrome 配置文件中，直到用户删除对话、在设置页清空对话与日志，或清除扩展数据。清空日志不会删除单独保存的模型配置和 API 凭据；这些数据可通过 Chrome 的扩展存储管理或卸载扩展清除。用户明确要求保存的截图、视频、PDF 等下载文件，需在 Chrome 下载目录自行删除。Surf Wax 不设置对话自动到期时间。

## Chrome 权限

Surf Wax 声明广泛的浏览器权限和 `<all_urls>` 网站访问权限，以便智能体按用户指令操作不同网页和浏览器功能。扩展不会因为安装了权限就自动执行所有对应操作。对特定 API 的调用取决于用户任务、模型生成的工具调用以及 Chrome 是否提供该能力。用户可随时停止当前任务、关闭 Side Panel 或卸载扩展。

## 联系方式

隐私问题可通过 [Surf Wax GitHub Issues](https://github.com/notCorwin/SurfWax/issues) 联系维护者。提交问题时请勿附上 API 凭据或未经处理的私人网页与完整日志。
